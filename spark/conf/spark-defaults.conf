# Purpose: Spark defaults for local F1 lakehouse (packages, S3A, Delta, logging).
# Tip: Update paths for Windows (e.g., C:\tmp\ivy) or use WSL paths; set AWS creds if connecting to S3.

# Where Spark/Ivy downloads Maven dependencies
# - Defaults to /tmp/ivy (change to a Windows path or a persistent path if needed)
spark.jars.ivy=/tmp/ivy

# Delta + S3A (match Spark's Hadoop core = 3.3.4)
# - Packages loaded via Ivy: Delta Lake, Hadoop AWS, AWS SDK bundle
spark.jars.packages=io.delta:delta-spark_2.12:3.2.0,org.apache.hadoop:hadoop-aws:3.3.4,com.amazonaws:aws-java-sdk-bundle:1.12.262

# S3A behaviour (no secrets, no endpoints)
# - Use path-style access for S3-compatible endpoints (e.g., MinIO/localstack)
spark.hadoop.fs.s3a.path.style.access=true
# - Ensure the S3AFileSystem implementation matches the hadoop-aws version
spark.hadoop.fs.s3a.impl=org.apache.hadoop.fs.s3a.S3AFileSystem

# Delta integration
# - Enable Delta SQL extensions and set the Spark catalog to Delta's catalog
spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog=org.apache.spark.sql.delta.catalog.DeltaCatalog

# Logging: point driver/executor JVM to a log4j2 configuration file
# - Ensure /opt/spark/conf/log4j2.properties exists in the runtime (adjust path for local Windows)
spark.driver.extraJavaOptions=-Dlog4j.configurationFile=/opt/spark/conf/log4j2.properties
spark.executor.extraJavaOptions=-Dlog4j.configurationFile=/opt/spark/conf/log4j2.properties